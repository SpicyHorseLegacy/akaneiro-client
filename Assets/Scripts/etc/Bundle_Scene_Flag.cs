using UnityEngine;
using System.Collections;

// when create current scene bundle. something in the scene would be bundled. paste this script on these things which is being bundled.
// and when loading bundles at runtime, loadbundle_tool would know these things need to be put into the scene.
public class Bundle_Scene_Flag : MonoBehaviour {
}
